import 'package:flutter/material.dart';

class TextImageBlock extends StatelessWidget {
  const TextImageBlock({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Bellaaaa!');
  }
}
